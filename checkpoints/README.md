# Checkpoints

Checkpoint files are not stored in Git.

Use:

```bash
python scripts/download_checkpoints.py --repo <hf-org-or-user>/argaze --output checkpoints
```

The expected files and hashes are listed in `manifest.json`.
